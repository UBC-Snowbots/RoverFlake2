name: test_workflow
on: push
jobs:
  demo:
    runs-on: ubuntu-22.04
    steps:
     - uses: actions/checkout@v4
     - run: |
         export ROVERFLAKE_ROOT="${HOME}/work/RoverFlake2/"
         yes | ./setup_scripts/setup_everything_common.sh
