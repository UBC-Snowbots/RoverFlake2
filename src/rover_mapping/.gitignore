# per-run outputs — never commit field data
missions/

# big binary imagery is regenerated by the pipeline scripts
imagery_pipeline/imagery/*
!imagery_pipeline/imagery/.gitkeep

# rendered per-site mapviz configs (template is the source of truth)
config/*.mvc
!config/mapviz_offline.mvc

__pycache__/
.pytest_cache/
