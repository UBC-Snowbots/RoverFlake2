string subsystem_host #the name of the computer running the system. Does not need to be set for requests, but used in feedback
string subsystem_name #the name of the subsystem
bool running #might want to change to an int, with more status 'states' than on/off
int16 index #also optional for requests, but used in feedback